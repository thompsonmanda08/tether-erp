// Stub for the `server-only` sentinel package in test environments.
// The real package throws if imported outside a Next.js server context;
// this stub does nothing so tests can import server modules safely.
export {};
